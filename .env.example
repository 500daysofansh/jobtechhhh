# Postgres connection
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sarkari_jobs

# Set true to run Playwright headless (false lets you watch it while debugging selectors)
SCRAPER_HEADLESS=true

# Optional: bump if a source is rate-limiting you
SCRAPER_REQUEST_DELAY_MS=1500
